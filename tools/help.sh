FONT_GREEN="\e[32m"
FONT_RED="\e[31m"
FONT_RESET="\e[0m"

clear
echo -e "${FONT_RED}$(figlet Fardo - Sec)${FONT_RESET}"

echo -e "You can exec with ${FONT_GREEN}fardo-sec${FONT_RESET}\n"
echo "${FONT_GREEN}Cameras${FONT_RESET}: Shortcut to acess traffic cameras."
echo "${FONT_GREEN}Sherlock${FONT_RESET}: Hunt down social media accounts by username across social networks."
echo "${FONT_GREEN}Mr.Holmes${FONT_RESET}: Mr.Holmes is a information gathering tool (OSINT)"
echo "${FONT_GREEN}SSH${FONT_RESET}: Connect to SSH servers."
echo "${FONT_GREEN}Swarm${FONT_RESET}: Multi-threaded DoS attack."
echo "${FONT_GREEN}Glitch${FONT_RESET}: Periodic automatic change of IP and MAC."
echo "${FONT_GREEN}Broker${FONT_RESET}: Router admin bruteforce + dictionary attack."
echo "${FONT_GREEN}Create Alias (shortcut)${FONT_RESET}: Create a custom shortcut in home directory."
echo "${FONT_GREEN}Update Alias (manually)${FONT_RESET}: Update aliases in home directory, sometimes you need to update to use new Alias."
